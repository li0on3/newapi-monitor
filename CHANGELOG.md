# Changelog

[简体中文](CHANGELOG.md) | [English](CHANGELOG_EN.md)

## Unreleased

## 1.9.0 - 2026-07-26

### Added

- 新增管理员告警投递中心，支持待投递、发送中、已送达、死信和已取消筛选，展示完整失败原因、尝试次数、下次重试时间、告警正文与关联事件。
- 新增单条/批量重试、取消和死信恢复操作；所有操作限制状态、限制批量大小并写入配置审计。
- 新增事件确认、全局静默时段和渠道计划维护窗口；严重告警可配置为绕过静默，维护结束后自动恢复探测与告警。
- 新增真实 FastAPI + SQLite + React 全栈 Playwright E2E，覆盖登录、投递操作、事件确认、动态设置和渠道维护配置。

### Changed

- 系统设置保存改为只提交实际变化字段，避免未启用渠道的默认占位值干扰其他配置保存。
- 告警投递记录增加优先级；静默期间的任务回到待投递状态且不消耗重试次数。

### Security

- 告警投递正文和失败详情仅管理员可读；状态变更继续要求同源请求标记并拒绝修改发送中或已送达记录。
- 重试与取消使用 SQLite 即时事务和状态条件更新，防止投递工作线程与管理员操作竞争覆盖。

## 1.8.0 - 2026-07-25

### Added

- 新增 SQLite 持久化告警投递队列：事件先入库，再按邮件、企微和飞书目的地独立发送；失败使用指数退避，重启后继续投递，超过上限进入死信。
- 新增数据库容量、WAL、待投递与死信自检，并支持原始采样、已恢复事件和投递记录的独立保留策略及定期维护。
- 新增 Playwright 浏览器端 E2E，覆盖管理员深层路由、普通用户权限边界和退出后刷新不自动登录。

### Changed

- 监控核心拆分为状态机、协议校验、持久化状态与通知投递模块；系统设置目录也从主页面组件中独立，降低单文件耦合。
- 慢请求只在出现新日志样本时推进窗口和提醒；最新五次恢复正常即可恢复事件，不再被更早的严重样本长期阻塞。
- 资源趋势按请求时间范围分桶聚合，24 小时视图覆盖完整时间窗而不是截取最近固定条数。

### Fixed

- 修复渠道同步返回畸形结构时被误判为空渠道列表、慢但有效的真实探测被误判不可用、容器首次重启和 OOM 状态重复告警的问题。
- 禁用、删除、维护或关闭告警的渠道会自动恢复对应未关闭事件；历史重启事件不再永久拉低总体状态。

## 1.7.0 - 2026-07-25

### Added

- 数据看板新增按模型堆叠的交互式请求趋势图，可在请求数、Token 和额度之间切换，并展示 Top 模型、峰值、时间桶平均值、模型图例和悬停明细。
- 自定义密钥分组升级为多对多成员关系：一个 API 密钥可以同时属于多个统计分组；点击分组卡片即可搜索、勾选并批量保存成员。
- 监控总览加入普通用户业务导航，并复用管理员配置的独立“用户端总览”渠道可见性。

### Changed

- 今天、近 7 天和近 30 天快捷范围现在点击即查询，并通过请求序列保护避免较慢的旧响应覆盖新筛选结果。
- 数据范围标签现在始终使用接口实际返回的时间范围；单时间桶也会绘制可见数据点，趋势图同时支持键盘、触屏和无障碍数据表。
- 普通用户界面改为中性“API 服务中心”身份，不再展示 New API 品牌、模块标题或登录文案；管理员和运维员仍保留完整技术视图。
- 分组统计明确采用当前多重成员关系：重叠分组会分别计入同一密钥，账户总计始终按密钥去重，页面明确提示分组总计不可直接相加。
- 历史单分组成员表在启动时自动迁移为多分组复合主键，已有分组和成员关系无需人工处理；旧版 `group_id` 请求仍兼容。
- 浏览器标题和普通用户可能看到的账号服务错误统一采用中性文案；“其他模型”使用不可冲突的内部标识，不会再与真实同名模型混算。
- 浏览器没有现成账号 Session 时，登录页停留在中性服务中心并提示先完成账号登录，不再自动跳转到带上游品牌的页面。

### Security

- 普通用户只能读取为用户端开放的渠道卡片与对应状态汇总；监控日志、机器资源、事件、Key 查询和系统配置仍在服务端要求运维员或管理员。
- 普通用户渠道接口采用严格字段白名单，不返回内部探测配置、原始渠道名、最近调用日志或上游错误正文。
- 分组成员更新继续通过当前 Session 重新校验完整密钥所有权，并按 `user_id` 隔离；删除一个分组不会删除密钥或影响其他分组关系。
- 单分组到多分组的数据库迁移在显式事务中完成；失败会完整回滚并可在修复数据后安全重试。

## 1.6.1 - 2026-07-25

### Changed

- New API Admin 与 Root 账号现在统一映射为监控管理员；普通 New API 用户继续按登录会话实时识别，无需在监控平台重复创建账号。
- 普通用户登录后默认进入 New API 概览，并且只显示概览、数据看板、API 密钥和使用日志四个个人业务页面。
- Key 用量查询的最低角色收紧为运维员，历史 `viewer` 配置会按运维员解释，避免配置与接口权限不一致。
- “总览展示”配置同步简化为管理员/运维员使用的监控渠道清单；历史普通用户可见性字段继续保留在数据库和接口中，升级无需迁移。

### Security

- 监控总览、渠道、官方状态、监控日志、资源、事件、Key 查询和配置接口全部在服务端拒绝普通用户，不依赖前端隐藏菜单。
- 普通用户访问监控页面深层链接时会回到首个可用的 New API 个人页面，不能通过手动输入 URL 绕过权限边界。

## 1.6.0 - 2026-07-25

### Fixed

- 修复显式退出监控后，刷新页面仍被残留 New API Session 自动登录为其他账号的问题；退出状态现在在监控范围内持续生效，直到用户主动选择“使用 New API 账号登录”。
- 紧急管理员登录完成后必须重新读取服务端身份，不再由前端在身份读取失败时自行构造管理员状态。

### Changed

- 移除“客户控制台”总模块和二级侧栏，将概览、数据看板、API 密钥、使用日志提升为独立顶级模块，同时保留原有深层链接和 BFF 数据契约。
- 主导航明确拆分为 `New API` 与“监控”两个工作区；监控侧使用“监控总览”“监控日志”等名称，避免与 New API 业务页面混淆。
- 系统设置中的入口更名为“New API 功能页”，配置键与数据库结构保持兼容，不需要迁移现有配置。

### Security

- 新增独立、HttpOnly、SameSite=Lax 的监控 SSO 抑制 Cookie；退出监控不会删除、修改或伪造 New API `session` Cookie。
- 恢复 New API SSO 必须通过同源校验保护的显式 POST 操作，跨站请求不能静默解除退出状态。

## 1.5.0 - 2026-07-25

### Added

- 新增“跟随系统 / 浅色 / 深色”三态主题切换，自动识别浏览器配色偏好、实时响应系统主题变化，并在本地持久化用户选择。
- 登录页、初始化向导和已登录控制台统一提供主题入口；页面加载前即应用已选主题，避免首屏明暗闪烁。

### Changed

- 全站视觉语言对齐 New API 默认前端：采用蓝色主色、语义化表面与边框、紧凑顶部栏、左侧工作区导航以及一致的卡片、表单、表格、弹窗和状态组件。
- 将历史硬编码深色样式迁移为浅色/深色共享的语义化设计令牌，保留渠道、日志、资源、事件、配置和客户控制台的全部功能与深层链接。
- 优化桌面、平板和移动端导航密度、焦点状态、减弱动画偏好和浅色模式可读性。

### Security

- 首屏主题引导使用同源静态脚本，兼容现有严格 Content Security Policy，无需放宽 `script-src`。

## 1.4.0 - 2026-07-25

### Added

- 客户控制台 API 密钥页新增独立“密钥分组”：用户可以创建、重命名、着色、删除分组，并批量移动自己的密钥；该分组与 New API 原生模型路由/计费分组完全分离。
- 新增 1 天、7 天和 30 天个人密钥用量与分组汇总，按 New API 不可变 `token_id` 关联真实 `/api/data/flow/self` 数据，展示请求数、额度、Token 和调用模型。
- 新增本地分组关系表、用户隔离约束、分组操作审计、密钥所有权复验以及对应契约/回归测试。

### Changed

- API 密钥页重构为“账户汇总 → 分组用量 → 个人密钥明细”的信息层级，并明确区分监控密钥分组与 New API 路由/计费分组。
- 分组周期统计采用“当前归属”口径：移动密钥后，所选周期的分组汇总按新的当前归属重新计算，避免伪造无法从 New API 还原的历史分组快照。

### Security

- 分组和成员关系强制按当前 New API `user_id` 隔离；移动密钥前重新通过当前 Session 拉取本人完整密钥清单，不能把其他用户的 Token ID 写入自己的分组。
- 用量接口固定调用个人范围 `/api/data/flow/self`，即使当前用户是 New API 管理员也不会借此扩大到全局密钥用量。

## 1.3.0 - 2026-07-24

### Added

- 新增独立“客户控制台”，在不修改 New API 源码的前提下提供概览、数据看板、API 密钥和真实使用日志四个页面。
- 新增固定白名单 BFF：只使用当前用户已验证的 New API Session 和用户 ID 调用对应接口，不使用监控管理 Token 代替客户身份，也不提供通用代理能力。
- API 密钥支持创建、编辑、启停、删除、批量删除和限速的单次明文查看；明文不落库、不进入日志、审计或前端持久化存储。
- 系统配置新增客户控制台总开关、最低角色、页面开关、默认查询范围、写操作限速和明文查看限速。
- 新增客户控制台操作审计、New API 身份 ID 一致性校验、普通用户 30 天查询边界和管理员全局/普通用户个人数据范围映射。
- 新增 `/monitor/console`、`/monitor/console/analytics`、`/monitor/console/keys`、`/monitor/console/logs` 和 `/monitor/system/console` 深层链接。

### Changed

- 监控服务现在可直接识别 `/monitor/*` 前缀，源码运行和反向代理部署使用相同 URL，不再依赖额外的路径剥离规则。
- 前端 CI 在生产构建前执行 Bun 单元测试；Docker 镜像包含客户控制台 BFF 模块。

### Security

- 紧急管理员被明确禁止访问客户控制台；监控角色仅控制入口，不能提升 New API 原始权限。
- 客户数据响应统一使用 `Cache-Control: no-store`，写操作继续要求同源校验头，并对上游响应大小、超时、字段和路由进行限制。
- 普通用户日志响应移除管理员信息、审计信息、流状态和渠道名称；CSV 导出防止公式注入。
- 客户控制台、初始化向导和 Key 查询链路不再携带凭据跟随 HTTP 重定向，并限制 SSO、初始化和 Key 查询响应体大小。
- 反向代理示例固定覆盖真实来源地址，避免客户端伪造 `X-Forwarded-For` 绕过来源审计和限速。

## 1.2.2 - 2026-07-23

### Fixed

- 兼容 OpenAI 官方状态接口在没有活动事件时省略 `incidents` 字段的响应，避免官方状态采集器持续失败并将平台健康检查误判为异常。

## 1.2.1 - 2026-07-22

### Changed

- 将 OpenAI 官方状态从总览大型区域迁移到独立的 `/monitor/upstream-status` 页面，总览仅保留可点击的轻量参考提示，渠道卡片重新成为首要信息。
- 将“本地真实渠道”明确设为主要判断信号，将 OpenAI 全局状态设为辅助上下文；无关官方事件不再表现为本地渠道故障。
- 即使管理员选择让官方状态影响总览，也只有已关注的业务组件异常才会影响 `OVERALL STATUS`。
- 优化桌面、窄屏和移动端导航布局，新增页面不会导致菜单换行重叠。

## 1.2.0 - 2026-07-22

### Added

- 接入 OpenAI 官方状态页，定时采集平台状态、组件状态、活动事件及官方进展时间线。
- 总览新增独立的“OpenAI 官方状态”区域；默认不参与本地渠道总览状态，避免把上游公告误判为本地故障。
- 系统配置新增“上游官方状态”页面，可配置采集频率、超时、告警影响级别、连续确认次数、组件范围和角色可见性，并支持实时连接测试。
- 官方事件与本地 OpenAI 模型渠道健康度关联展示，事件中心支持官方事件筛选、详情和恢复依据。

### Security

- 官方状态采集仅访问代码内固定的 HTTPS 官方接口，并限制响应体大小、超时和数据结构，避免可配置 URL 引入 SSRF 风险。

## 1.1.0 - 2026-07-22

### Added

- 可校验 SHA-256 的 Linux 一键安装器，默认使用固定版本 GHCR 多架构镜像并仅监听回环地址。
- 首次启动初始化向导：一次性令牌 15 分钟有效，支持 New API 管理员凭据自动换取令牌或手工填写已有令牌。
- `monitorctl` 运维命令，覆盖状态、日志、诊断、在线备份、升级、回滚、重置紧急管理员、续期初始化令牌和安全卸载。
- GitHub Release 自动附带安装器、部署包和校验文件。

### Security

- New API 管理员密码只在初始化请求内存中用于换取令牌，不写入数据库、日志或响应。
- 初始化完成后接口自动关闭，并使用令牌哈希、有效期和失败限速降低未配置实例暴露风险。

### Added

- 渠道同步、渠道探测、日志和资源采集的新鲜度自检。
- 采集器异常与恢复事件、详细运行状态页面和 503 健康降级。
- 数据库敏感配置加密、Host 白名单和状态变更请求校验。
- 非 Root 容器、资源限制、只读文件系统和 Docker Socket Proxy 加固。
- `manage.py` 初始化、部署诊断和 SQLite 在线备份。
- GitHub Actions、CodeQL、Dependabot、贡献和安全文档。
- 事件调查中心：状态、级别、类型、时间和全文筛选，事件队列与调查详情双栏工作区，以及恢复时长统计。
- 事件同时保留原始触发原因和恢复依据，支持历史数据兼容迁移。
- 系统配置新增“总览展示”，可分别控制管理端与普通用户看到的渠道，并按角色独立计算总览状态。
- 企业微信自建应用、企业微信群机器人、飞书自建应用、飞书群机器人和邮件统一通知分发。
- 通知中心支持动态配置、秘密字段加密和每个渠道独立触发真实测试告警。
- 按 API Key 查询真实用量、模型限制和最近调用详情，并提供角色与频率控制。
- 前端使用独立 History API 路由，支持深层链接、刷新和浏览器前进后退。
- 中英文 README、贡献指南、安全策略和路线图。
- Git tag 自动生成 GitHub Release，并发布带 SBOM/provenance 的 GHCR 多架构镜像。

### Fixed

- 渠道探测改为独立工作线程并支持有限并发，避免慢探测阻塞日志、资源和渠道同步采集。
- 渠道故障与恢复增加连续次数确认；临时 5xx、429 和超时先按降级告警，减少单次波动造成的告警抖动。
- 多渠道同时出现相同认证或分组权限错误时合并为探测凭证事件，不再误报为多个上游渠道故障。
- 渠道同步工作线程直接记录采集新鲜度，避免主循环未及时消费队列造成错误的采集器过期告警。
- New API 使用日志分页参数改为 `p`，避免回填和高流量场景漏日志。
- 渠道卡片时间与跳转箭头重叠。
- 总览健康状态、请求统计和渠道事件严格按界面启用的渠道范围计算。
- 运行中的容器会自动清理历史遗留的错误事件，避免状态误报。
- 普通 New API 用户默认映射为只读总览角色，日志、资源、事件和配置接口继续执行服务端权限校验。
- 修复事件恢复时覆盖原始告警内容，导致无法解释“为什么发生”的问题。
